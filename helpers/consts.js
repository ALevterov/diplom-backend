const path = require('path')

// const SQUID_LOGS_PATH = path.join('/var', '/log', 'squid', 'access.log')
const SQUID_LOGS_PATH = path.join('C:/', 'access.log')

module.exports = {
  SQUID_LOGS_PATH,
}
